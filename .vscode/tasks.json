{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Blog: git add/commit/push (auto msg)",
      "type": "shell",
      "command": "git add . && (git diff --cached --quiet && echo \"No staged changes.\" || (git commit -m \"post: ${fileBasenameNoExtension}\" && git push))",
      "problemMatcher": []
    },
    {
      "label": "Blog: Local Jekyll serve (optional)",
      "type": "shell",
      "command": "bundle exec jekyll serve --livereload",
      "problemMatcher": [],
      "options": {
        "cwd": "${workspaceFolder}"
      }
    }
  ]
}
